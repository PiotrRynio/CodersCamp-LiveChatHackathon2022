import React from 'react';
import { Wrapper } from './TopBar.styled';
import { Logo } from '../Logo';

export const TopBar = () => {
  return (
    <Wrapper>
      <Logo />
      topBar
    </Wrapper>
  );
};
